## Description

Used to store third-party script resources. Third-party scripts are usually installed through npm install, except for some scenarios, for example:

When using echart to implement chart development, if you introduce echart via npm install, the full version's size is too large. The official support to download the special chart you need online, so you can put the custom downloaded echart in this directory.